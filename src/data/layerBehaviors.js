export {
  buildLayerSchema,
  resolveGeometryType as guessGeometryType,
  resolveLayerBehavior as buildLayerBehavior,
  resolveLayerName as buildLayerName,
  resolvePopupSchema as guessPopupSchema,
  resolveStyleRef as guessStyleRef,
  slugify,
} from "./layerSchema";
